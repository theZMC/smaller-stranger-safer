# Deliberately committed. This file exists so the talk's dive demo can show
# a naive COPY . . shipping secrets into an image layer. Every value is fake.
DATABASE_URL=postgres://orders:hunter2@db.internal:5432/orders
API_TOKEN=fake-demo-token-do-not-panic
SESSION_SECRET=correct-horse-battery-staple
